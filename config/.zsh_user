export LANG=ja_JP.UTF-8

alias l="ls --color --show-control-chars"
alias ls="l -F"
alias ll="ls -l"
alias lll="ll -at"

alias du="du -h"
alias df="df -h"

setopt auto_pushd
setopt list_packed
setopt correct

# autoload predict-on
# predict-on

if [ $SHLVL = 1 ];then
    screen -xR
fi

